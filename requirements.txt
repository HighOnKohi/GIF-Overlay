# Core dependencies
PyQt6>=6.5.0
numpy>=1.24.0
Pillow>=9.0.0

# Optional — uncomment for additional features:
# opencv-python    # Video file support (MP4, AVI, MOV, etc.)
# rembg            # AI-powered background removal (auto-downloads ~170 MB model)
